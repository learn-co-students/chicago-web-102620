<h1>Menu</h1>

<% @nachos.map do |nacho| %>
  <ul>
    <li>
      <%= link_to nacho.name, nacho %>
      $<%= nacho.price %>
      <%=
        button_to "Add To Cart",
        add_to_cart_path,
        method: :patch,
        params: { nacho_id: nacho.id, name: nacho.name }
      %>
    </li>
  </ul>
<% end %>
